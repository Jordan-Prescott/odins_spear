from tqdm import tqdm

import odins_spear.logger as logger

def main(api: object, service_provider_id: str, group_id: str, number: str, 
         number_type: str, broadworks_entity_type: str):
    
    pass