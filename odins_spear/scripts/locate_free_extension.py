# 1. Fetching User Information: Retrieve all users within a specified service provider or group.
#Get all active Users, Alias, HG, CC and AA in a group.
#api.get.users(), api.scripter.find_alias(), api.get.group_hunt_groups(), api.get.group_call_centers(), api.get.auto_attendants()
#Will require ServiceProvider_id, Group_id


# 2. Filtering Available Extensions: Detect unassigned or "free" extensions.
#Filter from provided range.

# 3. Output Free Extensions: Likely an output in terminal showing all exts available. If in an easily format to be used in other script.

